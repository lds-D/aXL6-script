local CoreGui = game:GetService("StarterGui")

CoreGui:SetCore("SendNotification", {
    Title = "欢迎白名单用户",
    Text = "aXL6反挂机开启中",
    Duration = 5, 
})
local a=game:GetService("UserInputService")local b=game:GetService("VirtualInputManager")local c=game:GetService("RunService")local d=600 local e=10 local f=0.1 local g=0 local h=false local function i()g=0 end a.InputBegan:Connect(function(j,k)if not k then i()end end)c.Heartbeat:Connect(function(k)if h then return end g=g+k if g>=d then h=true local l=workspace.CurrentCamera local m=l.ViewportSize local n,o=m.X/2,m.Y/2 local p=a.TouchEnabled for q=1,e do if p then b:SendTouchEvent(n,o,0,true,game)b:SendTouchEvent(n,o,0,false,game)else b:SendMouseButtonEvent(n,o,0,true,game,0)b:SendMouseButtonEvent(n,o,0,false,game,0)end task.wait(f)end i()h=false end end)
CoreGui:SetCore("SendNotification", {
    Title = "反挂机",
    Text = "aXL6反挂机已开启",
    Duration = 5, 
})
local Start = tick()
local ui = loadstring(game:HttpGet("https://pastebin.com/raw/3vQbADjh"))()
local win = ui:new("aXL6脚本")
local UITab1 = win:Tab("通知","7734068321")
local about = UITab1:section("📢公告",true)
about:Label("本脚本永久公益倒卖死全家全家送进火葬场!😂😂😂")
about:Label("开源死全家全家骨灰都被风吹走了!😂😂😂")
local about = UITab1:section("📢更新",true)
about:Label("暂时还没更新请等更新了再来看")
local UITab1 = win:Tab("必开","7734068321")
local about = UITab1:section("通用",true)
about:Toggle("aXL6绕过反作弊V1.0", "开关", false, function(v)
    local function layer1()
        if getgenv then getgenv().debug_mode = false end
        local mt = getrawmetatable(game)
        local old = mt.__index
        mt.__index = newcclosure(function(t, k)
            if k == "IsLoaded" or k == "WaitForChild" then
            end
            return old(t, k)
        end)
    end

    local function destroyAntiCheatModules()
        local services = {"ReplicatedStorage", "Workspace", "Players"}
        for _, srvName in ipairs(services) do
            local srv = game:GetService(srvName)
            for _, obj in ipairs(srv:GetDescendants()) do
                if obj:IsA("Script") or obj:IsA("LocalScript") or obj:IsA("ModuleScript") then
                    local src = obj.Source or ""
                    if src:lower():find("anticheat") or src:lower():find("ban") then
                        obj:Destroy()
                    end
                end
            end
        end
    end

    local function hookRemotes()
        local remotes = {"RE", "RF"}
        local rs = game:GetService("ReplicatedStorage")
        for _, name in ipairs(remotes) do
            local obj = rs:FindFirstChild(name)
            if obj then
                if obj:IsA("RemoteEvent") then
                    local oldFireServer = obj.FireServer
                    obj.FireServer = newcclosure(function(self, ...)
                        return oldFireServer(self, ...)
                    end)
                elseif obj:IsA("RemoteFunction") then
                    local oldInvoke = obj.InvokeServer
                    obj.InvokeServer = newcclosure(function(self, ...)
                        return oldInvoke(self, ...)
                    end)
                end
            end
        end
    end

    local function hideInjection()
        local env = getgenv()
        env["injector_handle"] = nil
        env["bypass_flag"] = nil
        debug.setconstant = nil
        debug.setupvalue = nil
        local connections = getconnections and getconnections(game:GetService("RunService").Heartbeat) or {}
        for _, Signal in ipairs(connections) do
            Signal:Disable()
        end
    end

    local function disguiseAsNormalPlayer()
        local uis = game:GetService("UserInputService")
        uis.InputBegan:Connect(function(input, processed) end)
        local player = game.Players.LocalPlayer
        player.CharacterAdded:Connect(function(char) end)
    end

    local function fullBypass()
        layer1()
        destroyAntiCheatModules()
        hookRemotes()
        hideInjection()
        disguiseAsNormalPlayer()
        for i = 51, 100 do end
    end

    task.spawn(fullBypass)
end)
local UITab1 = win:Tab("本地玩家功能","7734068321")
local about = UITab1:section("属性",true)
about:Textbox("移动速度", "tpwalking", "输入", function(king)
    local player = game:GetService("Players")
    local lplr = player.LocalPlayer
    local chr = lplr.Character
    local hum = chr and chr:FindFirstChildWhichIsA("Humanoid")
    
    if hum then
        local newSpeed = tonumber(king)
        if newSpeed then
            hum.WalkSpeed = newSpeed
        end
    end
end)
about:Textbox("跳跃高度", "tpwalking", "输入", function(king)
    local player = game:GetService("Players")
    local lplr = player.LocalPlayer
    local chr = lplr.Character
    local hum = chr and chr:FindFirstChildWhichIsA("Humanoid")
    
    if hum then
        local newJump = tonumber(king)
        if newJump then
            hum.JumpPower = newJump
        end
    end
end)
local about = UITab1:section("功能",true)
